<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentGallery">

    <fragment
        android:id="@+id/fragmentGallery"
        android:name="com.example.searchPaging.ui.fragment_gallery.FragmentGallery"
        android:label="Image Gallery"
        tools:layout="@layout/fragment_gallery" >
        <action
            android:id="@+id/action_fragmentGallery_to_fragmentDetails"
            app:destination="@id/fragmentDetails" />
    </fragment>
    <fragment
        android:id="@+id/fragmentDetails"
        android:name="com.example.searchPaging.ui.fragment_details.FragmentDetails"
        android:label="Image Details"
        tools:layout="@layout/fragment_details" >
        <argument
            android:name="photo"
            app:argType="com.example.searchPaging.data.unsplash.PixabayPhoto" />
    </fragment>
</navigation>